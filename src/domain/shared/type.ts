export type BaseItem = {
    id: number;
    name: string;
    iconUrl: string;
    createdAt: string;
};

export type BaseItemRow = {
    id: number;
    name: string;
    icon_url: string;
    created_at: string;
};
